# improve list view display

in list views (e.g. Activity List), do actually render as inline markdown, but show only the first line (i.e. cut at new line), or more precisely, the first non-empty line

#collection